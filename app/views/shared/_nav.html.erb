<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to '#HashTaggedIt', root_path, class: 'logo' %>
  
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto main-nav">
      <% if user_signed_in? %>
        <% if current_user.image %>
          <li class='nav-item'><%= image_tag current_user.image, class: 'avatar' %></li>
        <% end %>
        <li class='nav-item logout'><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
        <li class='nav-item dashboard'><%= link_to "Dashboard", dashboard_path, class: 'nav-link' %></li>
      <% else %>
        <li class='nav-item session'><%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %></li>
      <% end %>
    </ul>
  </div>
</nav>
